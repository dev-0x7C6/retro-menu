#define CMAKE_INSTALL_PREFIX "${CMAKE_INSTALL_PREFIX}"

#define LIBRETRO_CORE_PATH "${LIBRETRO_CORE_PATH}"
#define LIBRETRO_CORE_INFO_PATH "${LIBRETRO_CORE_INFO_PATH}"
#define LIBRETRO_FIRMWARE_PATH "${LIBRETRO_FIRMWARE_PATH}"
